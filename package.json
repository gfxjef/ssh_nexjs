{
  "name": "ssh-nextjs",
  "version": "1.0.0",
  "description": "Sistema de gestión de posts con editor WYSIWYG",
  "main": "index.js",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "test:upload": "cd frontend && node scripts/test-image-upload.js",
    "test:delete": "cd frontend && node scripts/test-image-delete.js"
  },
  "dependencies": {
    "formidable": "^3.5.4",
    "react-quill": "^2.0.0"
  },
  "devDependencies": {
    "@types/formidable": "^3.4.5",
    "form-data": "^4.0.2",
    "node-fetch": "^3.3.2"
  }
}
